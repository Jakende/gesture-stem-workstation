# ADR 0003: Deterministic technical UI

Status: accepted

The workstation applies the local `tech-ui-designsystem`: exact dark/light inversion tokens,
monospace typography, a four-pixel spacing grid, zero-radius geometry, one-pixel borders,
no shadows or gradients, and motion only where it communicates runtime state.

